# План

Обновлён: 23.09.2026

Здесь только то, что предстоит. Что уже сделано — в Журнале.

## Логика порядка

Выбрано человеком 19.09.2026 (Р-11).

**Разбор — раньше каркаса.** Список хранилищ версии 1 замерзает с первым релизом, а форма записи учёта без выгрузок банков — догадка (Р-02). Поэтому Этап 0 начинается не с кода.

**Первый этап заменяет месячный лист — и больше ничего.** Петля «внёс месяц — увидел, сколько отложил» до всего остального: без неё приложение повторит таблицу — запись есть, ответа нет (Р-07; «Трапеза», Р-01).

**Вторые вопросы — сразу за первым.** Обычный месяц, рост и необходимый доход стоят на той же истории и оживают в день переноса таблицы.

**Долги — раньше капитала.** Они чинят главный вопрос: без них оплата за компанию завышает расход. Капитал заменяет лист, который и сейчас работает руками. Этапы 3 и 4 друг от друга не зависят и меняются местами, если человек захочет.

**Синхронизация — в Этапе 1**, пока сущностей мало: ядро даёт её готовой.

---

## Этап 0 — Разбор выгрузок и каркас на ядре

Вопрос этапа: как приложение ставится на телефон и открывается без сети, собранное на `FamilyCore`, — и из каких записей состоит учёт?

**Часть А — разбор, до кода. Сделана 20.09.2026** (Р-12): выписки четырёх банков разобраны, семь вопросов отвечены, восемь развилок закрыты, типы и `v1Stores` в `02-Архитектура` окончательны. Xlsx зашифрован и не разбирался — история переносится по снимкам листов и руками, в Этапе 1.

**Часть Б — каркас.**

5. **Подключение ядра** по чеклисту `02-Архитектура`, «Как „Злотые“ подключают ядро». «Годится ли ядро новому приложению без `as`?» Нехватка — строкой для сессии `FamilyCore`, не обходом
6. **Модель и конфиг:** `app/model.ts` — типы дословно из Архитектуры, `SCHEMA_VERSION = 1`, пустые миграции; `app/config.ts`; `config.test.ts` — имя базы, `v1Stores`, раскладка
7. **`modules/money`:** «Правильно ли складываются и показываются деньги?» Знаки валют из `Intl`, таблица крипты, пересчёт по курсу на дату с основанием, округление — чистые функции с тестами
8. **Оболочка:** вкладки, пустой экран «Месяц», «Настройки» с синхронизацией и файлом-копией ядра, «Что нового», приветствие, справка со сторожем цифр
9. **Сборка и деплой:** `familyVite` с `base: '/Zloty/'`, манифест, иконки своими фигурами, работник, `deploy.yml` с `submodules: true`, Pages
10. **Прогон** `scripts/smoke.mjs`: открывается, вкладки, работа без сети; проверен порчей

**Готово когда:** форма учёта решена и записана, типы в Архитектуре без пометки «контур»; `npm test`, `npm run build`, прогон — зелёные по коду выхода; приложение установлено на телефон с `goigolden88.github.io/Zloty/` и открывается без сети; нехватки ядра, если были, названы строкой для Журнала `FamilyCore`.

## Этап 1 — «Месяц»: сколько я откладываю

Вопрос этапа: сколько я отложил в этом месяце, хватает ли дохода и что вышло за обычное?

**Порядок работ выбран человеком 20.09.2026:** ввод → импорт → «Месяц», то есть пункты 1, 2, 4, 6 первыми, а 5, 3, 7, 8, 9 — следом. Это тот же довод, которым «Логика порядка» объясняет сам Этап 1: петля «внёс месяц — увидел, сколько отложил» раньше всего остального. Регулярные проектируются после импорта — он покажет, как они выглядят в выписке; «обычный месяц» до переноса истории честно говорит, что считать не по чему. Отдельного решения Р-NN это не потребовало: Р-11 фиксировал состав этапов, а не порядок пунктов внутри них.

1. ~~**Счета и категории**~~ — сделано 20.09.2026: `modules/ledger`, экран «Счета и категории» ссылкой из шапки «Месяца», валюты и настройки учёта (`profile`) там же
2. ~~**Операции и итоги периода**~~ — сделано 20.09.2026: `entries.ts` с правилами модели, экран «Операции» вкладкой, правило «либо итог, либо операции» — тестами и прогоном
3. ~~**Регулярные**~~ — сделано 20.09.2026: `recurring.ts`, шаблоны в справочниках, блок на «Операциях», «Внести» и «Внести все», выписка отмечает сама полем `recurring`
4. ~~**Импорт учёта**~~ — сделано 20.09.2026: пять разделов, `ext` и ловля повтора, склейка переводов внутри файла, замена итога операциями, промпт со справочниками и датой последней операции, экран «Загрузить выписку», `registry.ts`
5. ~~**История таблицы**~~ — сделано 20.09.2026, девять периодов из девяти: `scripts/check-history.mjs` сверяет четырьмя проверками и собирает файл импорта. Суммы сошлись со столбцом «Сумма» и с итоговым листом; опечатку в границах девятого периода поймала проверка расхода в день
6. ~~**Экран «Месяц»**~~ — сделано 20.09.2026: отложено и норма сбережений, обычный месяц по месяцам и периодам, «вышло за обычное», столбики; каждое число с основанием, доход без записей назван словами
7. ~~**Лента и markdown**~~ — сделано 20.09.2026: строки ленты и выгрузка в `registry.ts`; отдельной вкладки «Лента» нет (Р-15) — поиск живёт в «Операциях», markdown — в «Настройках»
8. ~~**Синхронизация с `ZlotyData`**~~ — сделано: код 20.09.2026, живая проверка 21.09.2026. Человек завёл токены на обоих устройствах и проверил обмен между телефоном и компьютером
9. ~~**Напоминания**~~ — сделано 20.09.2026: две темы поверх `createReminders` — «месяц не внесён» и «регулярные не внесены», правила в `remind.ts` с тестами, раздел в «Настройках»

**Готово когда:** итоги истории по периодам сходятся с таблицей до рубля; новый месяц внесён выпиской и регулярными, экран «Месяц» отвечает с основанием; запись с телефона видна на компьютере; человек говорит, что лист этого месяца в Excel не вёл.

**Этап закрыт 22.09.2026.** Четыре условия из пяти выполнены: история сходится до рубля; сентябрь внесён настоящими выписками четырёх банков — 168 операций; «Месяц» отвечает с основанием; обмен между телефоном и компьютером проверен человеком. **Пятое выполнено наполовину:** месяц внесён выпиской, но не регулярными — ни одного шаблона человек не завёл. Закрыто осознанно, оговорка уходит в Этап 2, где регулярные нужны для необходимого дохода.

Живая проверка выписки дала пять решений — Р-16 (сверка по числам самой выписки), Р-17 (движения внутри счёта объявляются суммой), Р-18 (дата операции против даты обработки), Р-19 (склейки встречных сторон нет), Р-20 (период владеет месяцем по большинству дней). Ни одну из этих ошибок не нашли бы тесты.

## Этап 2 — «Какой доход мне нужен»

Вопрос этапа: какой доход мне нужен и как растут мои обычные расходы?

0. **Регулярные — завести** (хвост Этапа 1): шаблоны написаны и работают, но ни одного не заведено, а необходимый доход стоит на них. Кандидаты видны в загруженной выписке — связь показывается дважды в месяц
1. ~~**Необходимый доход**~~ — сделано 22.09.2026: `need.ts`, блок на «Месяце» под обычным месяцем. Редкие регулярные выносятся из обычного месяца и возвращаются долей (Р-27); чего вынести нельзя — итоги прежней таблицы — названо числом
2. ~~**Рост обычного месяца**~~ — сделано 22.09.2026: поздняя половина окна против ранней, в целом и по категориям (Р-28). Блок на «Месяце», а не отдельный экран, — отступление от Р-07 с доводом. По категориям рост стоит только на месяцах: у итогов прежней таблицы категорий нет, и число месяцев называется
3. ~~**«Что улучшить»**~~ — сделано 22.09.2026: `report.ts`, блок «Что улучшить» на «Месяце» с кнопкой «Скопировать отчёт». Числа месяца, вопрос в конце и обязательный раздел «чего приложение не знает» (Р-29)

**Готово когда:** на истории таблицы оба числа видны с основанием; человек разобрал отчёт месяца в беседе и сказал, годится ли.

**Этап закрыт по коду 22.09.2026.** Первое условие выполнено: необходимый доход и рост видны с основанием на настоящей истории. **Второе осталось за человеком** — разбор отчёта в беседе не проводился.

Девять решений — Р-21…Р-29, и шесть из девяти вызваны настоящим экраном, а не планом. Правило «насколько месяц записан» переписывалось четырежды за день: Р-22 (по календарю), Р-24 (по записям), Р-25 (равные сроки), Р-26 (факт из выписки). Две догадки подряд провалились на первых же данных, потому что записи знают, что случилось, а не что загружено.

Пункт 0 — регулярные — выполнен наполовину: человек завёл один шаблон. Доходных нет и не будет: стипендия кончилась.

## Этап 3 — Долги

Вопрос этапа: кто мне должен, кому я, и сколько из оплаченного — действительно мой расход?

Разбор сделан 22.09.2026: оба открытых вопроса закрыты до кода — **Р-30** (типы: событие сущностью, доли поимённо в трате, перевод комнаты с заметкой) и **Р-31** (доля в учёте: категория своя, в поток входит доля, а не сумма).

1. ~~**Люди и одноразовые долги**~~ — сделано 23.09.2026: вкладка «Долги», сверху итог «кто кому должен» по всем комнатам и разовым долгам сразу, быстрый ввод и возврат частями (Р-30)
2. ~~**Комнаты и события**~~ — сделано 23.09.2026: комната своим адресом, события со своим составом, траты с поимёнными долями, переводы с заметкой о банке, «кто кому должен» с кнопкой «Перевели», итог друзьям текстом. Шесть условий Р-01 — тестами, два из них сторожем по исходнику (Р-30, Р-32)
3. ~~**Интеграция с учётом**~~ — сделано 23.09.2026: `src/summary/`, кнопка «Связать» на «Операциях», доля вместо суммы во всех расчётах «Месяца» через один `toBase`, две строки в отчёте месяца (Р-31)
4. ~~**Миграция** на версию 2~~ — сделано 22.09.2026: семь хранилищ, `additive: true`; база компьютера с настоящими данными открылась версией 2, учёт цел

**Готово когда:** настоящая комната и одноразовый долг внесены; «сколько мне должен Петя» — по всему сразу; оплата за компанию входит в «Месяц» только своей долей; телефон открывает базу после миграции без переустановки.

**Этап закрыт по коду 23.09.2026.** Второе и третье условия выполнены кодом и проверены прогоном на всём пути — от пустого экрана до рассчитавшейся комнаты и связанной операции. **Первое и четвёртое остались за человеком:** разовый долг на настоящей базе заведён при проверке экрана, настоящая комната — нет; миграция проверена на компьютере, телефон — нет.

Решения — Р-30…Р-33. Два открытых вопроса закрыты до кода по образцу «Верните Деньги»; Р-33 — доли весами — заказал экран уже после закрытия. Прогон нашёл две настоящие ошибки экрана, которых не видно рассуждением: фолд, сворачивавшийся сам при первом человеке, и числа, потерянные рядом со склонениями.

## Этап 4 — Капитал

Вопрос этапа: сколько у меня всего и как это растёт?

Разбор листов сделан 23.09.2026, до кода: четыре листа сходятся между собой на всех датах; цена BTC и курс EUR жили внутри формулы и с марта не менялись; стипендия лежала в капитале вторым разом; деньги с кредитки считались капиталом. Решения — **Р-34** (перенос как был), **Р-35** (кредитка — отложенный платёж), **Р-36** (заметка к капиталу, схема 3), **Р-37** (пересчёт через промежуточную валюту), **Р-38** (публичный источник курсов через импорт), **Р-39** (снимок строкой, капитал на дату).

**Порядок работ принят человеком 23.09.2026** — так, чтобы экран он увидел уже на перенесённой истории:

1. ~~**Пересчёт через промежуточную валюту**~~ — сделано 23.09.2026 (Р-37): `findPath`, прямой курс главнее; «Месяц» получает его же
2. ~~**Заметки и миграция на версию 3**~~ — сделано 23.09.2026 (Р-36): `Note`, хранилище `notes`, `additive: true`; базы версий 1 и 2 открываются версией 3
3. ~~**Капитал чистыми функциями**~~ — сделано 23.09.2026 (Р-35, Р-39, Р-40): `capital.ts`, `row.ts`, `summary/capital.ts` с долгами через `debtsOn`
4. ~~**Импорт**~~ — сделано 23.09.2026: разделы `balances` и `notes`, `unit` у валют, `source` у курсов
5. ~~**Перенос листов**~~ — сделано 23.09.2026 (Р-34): `npm run capital` — 135 проверок через настоящие `planImport` и `capitalOn`; человек загрузил файл, экран показал 375 876,88 ₽ на 01.09.2026 — ровно число сверки
6. ~~**Экраны**~~ — сделано 23.09.2026: вкладка «Капитал», «Новый снимок» строкой, заметки, столбики, единица показа и «Изменить» у валюты. По экрану человека — Р-41 (валюта по умолчанию) и три правки показа
7. ~~**Курсы**~~ — сделано 23.09.2026 (Р-38): курс руками, «Подтянуть курсы» с запасным адресом; живой запрос через код приложения из node ответил. **Из браузера — не проверено**
8. ~~**Закрытие**~~ — сделано 23.09.2026: справка, Журнал, передача

**Готово когда:** история листов перенесена и сходится — по каждому счёту до рубля, а капитал со «Итоговым» с поправками на стипендию и кредитку, каждая названа суммой (Р-34); снимок этого месяца внесён; капитал в двух валютах с основанием; курс хоть раз подтянут из источника живым запросом; «Основная выжимка» больше не ведётся.

**Этап закрыт по коду 23.09.2026.** Первое и третье условия выполнены и проверены человеком на настоящей базе: восемь дат перенесены, капитал в ₽ и USD с основанием. **За человеком:** снимок этого месяца; первый «Подтянуть курсы» из браузера; «Основная выжимка» больше не ведётся.

Решения — Р-34…Р-41. Шесть приняты до кода, два — по ходу: Р-40 (курс на дату капитала — отступление от Р-04, названное до того, как закрепилось) и Р-41 (валюта по умолчанию — с экрана человека).

## Все этапы закрыты по коду — что осталось

Этапы 0–4 по `Р-11` сделаны. Живые проверки за человеком — в Журнале по этапам. Отложенные правки ядра сделаны 23.09.2026 сессией `FamilyCore` (Я-07, Я-08) и применены здесь: исходы импорта своими словами, «Подтянуть курсы» на сводке ядра; выборки по индексу пока не нужны нигде (Р-43), сторож «расход в день» остался в сверке истории (Р-42).

Следующих этапов в Р-11 нет. Всё, что задумано сверх них, — в «Отложено» ниже, с условиями возврата; этапом оно становится сессией планирования и новым решением.

## Вопрос, который задаётся до кода

**Что делать, если ядру не хватит.** «Злотые» — первое приложение, которое начинается на ядре (Р-03). Всё, чего не хватит, — строкой человеку и сессией `FamilyCore`, а не `as`, копией файла или правкой `src/shared`.

## Отложено

| Что | Почему не сейчас | Когда вернуться |
|---|---|---|
| Комнаты с участниками по ссылке (Р-01, вариант Б) | нужен сервер и второй канал синхронизации | когда комнаты поживут и друзья захотят вносить сами |
| Портфели, стратегии, ребаланс, API брокеров (Р-09) | «вложения как есть» — снимками; портфель — другой модуль | после Этапа 4, когда снимки вложений проживут несколько месяцев |
| «Входящие» — файлы импорта в репозитории данных (Р-09) | источник пока один — беседа | с первым источником сверх беседы; скорее ядро — Я-NN |
| Запланированные расходы | лист ещё не ведётся | когда человек начнёт; кандидат — `Recurring` «один раз» |
| «Похоже на регулярную?» (Р-06) | нет истории операций | через полгода операций |
| Сверка потока и остатка (Р-05) | нужны и операции, и снимки | после Этапа 4 |
| Сравнение с официальной инфляцией (Р-07) | по желанию | после Этапа 2 |
| Отдать другу | вместе с семьёй приложений | как Этап 7 «Трапезы» |
| Сторож «расход в день» в разборе импорта (Р-42) | итоги периода приходят только из скрипта истории, а выписки держит сверка Р-16 | когда итог периода придёт не из скрипта истории — например, таблица друга |
