# Процесс

Обновлён: 24.09.2026

Как ведётся работа над проектом. Не про то, что строим, а про то, как.

## Где что живёт

| Что | Где |
|---|---|
| Код приложения | `github.com/goigolden88/Zloty`, публичный; сайт — `goigolden88.github.io/Zloty/`. Локально — `C:\Ф\Prognananda\Claude Projs\Злотые` |
| Документы проекта | `docs/` в том же репозитории |
| Инструкции для Claude Code | `CLAUDE.md` в корне |
| Данные пользователя | IndexedDB на устройстве; с Этапа 1 — приватный репозиторий `ZlotyData` (заводит человек) |
| Таблица, выписки, копии, файлы импорта | `seed/` в корне — под `.gitignore`, в репозиторий не попадают никогда |
| Общее ядро и договор семьи | `github.com/goigolden88/FamilyCore`, публичный — эталон ядра. Здесь — подмодулем в `src/shared` с Этапа 0; свой клон для сессий ядра — `C:\Ф\Prognananda\Claude Projs\FamilyCore` |
| Приложения семьи | «Трапеза» `Еда\Trapeza`, «Делу Время» `ТаймМенеджмент\DeluVremya`, «Дневники» `Дневники\Dnevniki` — **только читать** |

Документы лежат вместе с кодом: `CLAUDE.md` подхватывается автоматически, документы версионируются заодно с кодом. Копия в чате — снимок; правится только репозиторий.

### Сессии Claude Code

Запускать из корня `Злотые`: история бесед привязана к рабочей папке. Сессия планирования 18–19.09.2026 шла отсюда же. Между этапами — `/clear`, `/compact` — для середины этапа. Перед `/clear`: дерево чистое, всё запушено, и по одному Журналу понятно, где мы и что дальше.

### Соседи — только читать

Из репозиториев «Трапезы», «Делу Время», «Дневников» и `FamilyCore` ничего не правится и не коммитится, даже найденная ошибка: о ней говорится человеку. **Сравнивать их код — через `git show`**, никогда по файлам с диска: `core.autocrlf=true` разворачивает рабочие копии в CRLF.

### Подмодуль ядра

С Этапа 0 (Р-03).

- **Клон** — только `git clone --recurse-submodules`; склонированное без него — `git submodule update --init`. Иначе `src/shared` пустая, и сборка падает непонятной ошибкой
- **`src/shared` отсюда не правится.** Ядру чего-то не хватает, в нём ошибка, `db` или `sync` не подходят под его типы без `as` — сказать человеку, что и где, дать промпт для сессии сопровождения `FamilyCore` (шаблон — в её `06-Процесс`) и остановиться на этом месте
- **Обновление ядра здесь** — `git submodule update --remote src/shared`, затем тесты, сборка, прогон по коду выхода и **коммит указателя**. Видимое изменение ядра — строка в `src/changes.ts` в том же коммите
- **Перед пушем** — `git status` чистый **и в подмодуле тоже**: `git -C src/shared status`
- **В `src/shared` не запускать `npm install`** — вторая копия React (Я-02 «FamilyCore»)
- **Тесты ядра здесь не гоняются, типы — проверяются** — как Р-52 «Трапезы»: `vitest` исключает `src/shared/**`, `tsc` проверяет его целиком
- Поломка в ядре чинится один раз, в `FamilyCore`

## Разделение работы

**Claude Code — и «что строить», и «как».** Продуктовые развилки и пересмотры архитектуры — тоже здесь: он видит настоящие файлы и проверяет факты. Режим планирования — когда обсуждаем и ничего не трогаем.

**Чат (Claude Desktop, claude.ai) — когда компьютера нет под рукой**, для свободного разговора без кода и для картинок. Итог приходит в репозиторий готовыми кусками markdown.

**Беседа с Claude — ещё и инструмент данных.** Выписки, скриншоты и файлы разбираются вне приложения по его промпту; в приложение приходит готовый JSON `zloty-import` (Р-08, Р-09). Выписки из беседы в репозиторий не кладутся — только в `seed/`.

### Планирование в Claude Code

1. Сессия планирования из корня репозитория — заготовка в «Шаблонах»
2. Решения — в `05-Решения` в момент выбора; правки `01`, `02`, `03` — по ходу
3. Конец: Журнал, передача, коммит документов, дерево чистое
4. `/clear` — и код новой сессией по плану. Контекст планирования в неё не переезжает: память — документы

## Обратный поток: код → обсуждение

То, что Claude Code в конце сессии называет «не описано в документах», возвращается в обсуждение, а не решается на месте.

## Правила ведения документов

- **Не менять задним числом.** Отмена решения — новая запись со ссылкой
- **Решения записываются в момент выбора**
- **Журнал обновляется в конце каждой сессии**
- **Перед пушем — `npm test`, `npm run build`, `npm run smoke`**, итог по коду выхода, а не по выводу
- **Прогон проверяется порчей**, когда меняется его сценарий: сломанная строка экрана обязана дать код 1. Порча — в `git worktree`, не в основной папке
- **Worktree порчи получает `node_modules` ссылкой-junction** — и перед удалением ссылка снимается первой (`cmd /c rmdir <worktree>\node_modules`), только потом `git worktree remove`. `--force` по живой ссылке вычищает зависимости основной папки (Журнал 23.09.2026)
- **Рабочие копии, прошедшие через git, — в CRLF:** worktree порчи и файлы после `git stash pop`. Образец замены — без перевода строки или с приведением к LF; скрипт порчи падает, если образец не найден, а `git diff --stat` показывает, что порча легла. Иначе прогон честно проходит на целом коде (Журнал 23.09.2026, вечер). Подмодуль в worktree — `git -C <worktree> submodule update --init`
- **Проверка идёт на замершем дереве:** пока фоновый прогон не кончился, код не правится — сборка возьмёт половину правки. Порции проверяются по отдельности, следующие — в `git stash`
- **Видимое изменение — строка в `src/changes.ts`** в том же коммите
- **Живые проверки напоминаются между этапами и в начале сессии** — списком из `03-План`, «Живые проверки»: человек делает их по ходу использования, а не отдельной работой (решено 24.09.2026)
- **План содержит только предстоящее**
- **Цифра с листа таблицы — вместе с формулой**, из которой она посчитана (опыт «Трапезы», Журнал 15.09.2026)
- **Правки файлов с кириллицей и замены с обратной косой чертой** — инструментом правки или скриптом-файлом из папки сессии, не heredoc Bash: он съедает `\` (опыт `FamilyCore`, Журнал 18.09.2026)

## Шаблоны

Сессия сопровождения `FamilyCore` — по нехватке ядра. Запускать **из корня `FamilyCore`**, не из «Злотых»; промпт называет поводы фактами из кода «Злотых» и просит решение Я-NN. Образец — промпт третьего исхода `ImportPlan`, из которого вышли Я-07 и Я-08 ядра: в истории этого файла до 23.09.2026. После релиза здесь — `git submodule update --remote src/shared`, тесты, сборка, прогон, коммит указателя и строка в `src/changes.ts`.

Начало сессии по плану:

```
Работаем по docs/03-План.md, следующий этап оттуда.
Прочитай CLAUDE.md и docs/ перед началом.
Сначала покажи план работ, код пиши после подтверждения.
```

Сессия планирования:

```
Планируем: <что — новый этап, модуль, развилка>. Прочитай CLAUDE.md
и docs/. Код не пишем — обсуждаем по одной развилке, с твоей
рекомендацией, и записываем решения в docs/ в момент выбора.
В конце — Журнал, передача в 06-Процесс, коммит документов.
```

Конец сессии:

```
Дай текст для docs/04-Журнал.md: что сделано, в каком состоянии,
следующий шаг. И для docs/05-Решения.md, если по ходу что-то решили.
Отдельно — что осталось не описанным в документах.
```

### Передача в следующую сессию

Переписывается в конце каждого этапа. Сейчас — середина разбора Этапа 6: **развилка 1 из шести решена (Р-49), кода Этапа 6 нет; срез итогов для «Тотального Учёта» отдаётся с 25.09.2026 (Р-50).**

```
Проект «Злотые» — личный учёт денег, четвёртое приложение семьи
local-first приложений, выросшее на ядре `FamilyCore`.
Работать в корне репозитория `C:\Ф\Prognananda\Claude Projs\Злотые`.
Клон — только `git clone --recurse-submodules`.

Прочитай `CLAUDE.md` и все файлы `docs/` перед началом — особенно
`04-Журнал.md` (записи 23–25.09.2026) и решения Р-34…Р-50.

Состояние: Этапы 0–4 закрыты по коду. Схема — версия 3 (долги — 2,
заметки к капиталу — 3). В базе настоящие данные: девять периодов
прежней таблицы, выписки четырёх банков, восемь дат капитала из листов.
Ядро — `cb72d16` (до Я-29): срез итогов, `meta.json` с `app`,
«Проверить доступ» не обещает запись. Срез «Злотых» — `summary/slice.ts`
(Р-50): записи, регулярные, снимки счётом, без сумм и названий.
`npm test` 652, `npm run build`, `npm run smoke` 166 — зелёное по коду
выхода 25.09.2026; `history` 44 и `capital` 135 — прошлых сессий.
Телефон открыл базу после двух миграций.

Живые проверки делаю по ходу использования — список в `03-План`,
«Живые проверки». В начале сессии коротко напомни, что там открыто.
Принесу находки — разбираем их первыми. Вычитку разведки ядра
(`FamilyCore` `docs/07-Разведка.md`, Я-10, Я-11) «Злотые» уже отдали —
срез итогов сделан (Р-50). Ступени 2–3 Я-13 — только по просьбе
потребителя, правкой состава договора; тогда же — вынос склейки
«Месяца» в чистую функцию (`03-План`, «Отложено»).

Наличные закрыты Р-44, BTC истории — курсы таблицы (Р-45), имена
в git остаются (Р-46), раскладка кода подтверждена (Р-47). **Открыт
один вопрос по коду** (`05-Решения`, «Открытые вопросы»): поле
«С какого числа считается месяц» сохраняет `periodStartDay`, а расчёт
его не читает — убрать поле или считать по нему; спросить меня, когда
будет к месту, не решать самому.

Этапы после Р-11 — Р-48. Этап 5 «первый полный месяц без Excel» —
моё использование, кода нет, кроме правок по моим находкам. **Работаем
Этап 6 — сверка потока и остатка, разбор до кода идёт** (`03-План`,
Этап 6, «Разбор — шесть развилок»). Развилка 1 решена — **Р-49**:
сверка по каждому счёту в его валюте, с раскрытием; рядом — общее
сравнение изменения капитала с «доход − расход», которое разницу
раскладывает, а «мимо записей» в нём — только то, что объясняют
расхождения по счетам. **Следующая — развилка 2:** срок сверки (между
какими снимками; день снимка — на начало или конец дня) и как узнать,
что выписки покрывают весь срок. Дальше 3–6 по одной, с рекомендацией.

Находки разбора, от которых зависят развилки:
— **начало загруженных выписок приложение не знает:** `loadedThrough`
  хранит только конец (Р-26), из записей начало не выводится (урок
  Р-24 → Р-26); без него незагруженные дни сверка сочтёт пропажей;
— **сверять сейчас есть что только у Тиньк, Яндекс, Сбер** — снимки
  и выписки; «Альфа» — выписка без снимка; наличные $ и €, кошелёк,
  площадки — снимки без операций;
— **первый живой ответ ждёт и снимка месяца, и выписки, доведённой
  до его даты** — сейчас выписки примерно по 18–20.09; до того этап
  проверяют тесты и прогон.

**Чему научила прошлая сессия (разбор Этапа 6).**
— До развилок — проверить, у каких счетов на деле есть и снимки, и
  выписки (файлы `seed/`, без сумм): это сузило этап до трёх банков.
— Выбор человека бывает шире рекомендации. Поправку, без которой
  выбранное соврёт на его данных, назвать при записи и пометить своей
  (Р-49, п. 3) — снять её можно новым решением.

**Чему научила сессия до неё.**
— Цену рекомендации проверять в коде до выбора человека: «ноль и архив
  без кода» на деле оставлял строку «0» на каждой дате (Р-44) —
  нашлось до решения, сказано, решение вышло честным.
— Подсказку «что проверить» сверять с данными человека: я предложил
  ему увидеть замену итога на своей выписке, а его итоги лежат на
  счёте истории, куда выписки не грузятся.
— Инструкция может разойтись с кодом: `CLAUDE.md` не знал про
  `summary/` (Р-47). Правило в инструкции — тоже проверяемый факт.
— Проверка — на замершем дереве. Прогон в фоне, пока правится код,
  собирает половину правки — его итог недействителен. Порции
  проверяются по отдельности, следующие — в `git stash`.
— Порча в worktree может не лечь молча: копия в CRLF. Скрипт порчи
  падает, если образец не найден; `git diff --stat` — что легла.
— Прежде чем читать по индексу, проверь, у всех ли записей есть поле:
  итог периода без `date` в индекс даты не попадает (Р-43).
— Сверка переноса, которая гоняет файл через настоящий `planImport`
  и считает настоящим `capitalOn`, проверяет и перенос, и код. node 24
  импортирует TS-модули приложения напрямую — так и делать дальше.
— Отступление от решения находится в коде (Р-40): как только нашлось —
  назвать и записать, до того как оно закрепилось.
— Цену изменения называть честно: «миграция — значительные изменения»
  было преувеличением, человек поймал. Двенадцать строк — так и сказать.
— Экран с настоящими данными показывает то, чего нет в прогоне:
  перевёрнутый курс второй валюты, дубль счёта наличных.

Что важно помнить про уже написанное:
— **капитал на дату — последний снимок каждого счёта не позже неё**
  (Р-39), пересчёт — по курсу на дату капитала (Р-40), через одну
  промежуточную валюту, прямой главнее (Р-37);
— **отрицательная часть снимка — отложенный платёж** (Р-35): в
  сбережениях не вычитается, капитал вычитает строкой;
— **модуль капитала не знает учёта и долгов** — сторож в
  `capital.test.ts`; сводит их `src/summary/capital.ts`, долги — через
  `debtsOn` из `debts-report.ts`, та же функция, что у отчёта месяца;
— **источник курсов — один файл** `money/currency-api.ts` (Р-38): ответ
  становится файлом `zloty-import` и идёт через `planImport`;
— **основная валюта не выбрана — первая заведённая** (Р-41);
— **счёт закрывается нулём и архивом** (Р-44): архивный счёт с нулём
  на дату в капитал не входит, раньше нуля — как был. «Наличные ₽»
  закрывается так человеком — касса живёт на «Наличных»;
— **курсы BTC и EUR истории — таблицы** (Р-45): первый снимок по
  рыночной цене даст скачок капитала, которого в этот день не было;
— **исходы импорта — своими словами** (Я-07 ядра): `notes` — загрузится,
  но стоит посмотреть; `changed` — правка записи из базы; `removed` —
  надгробие; `issues` — только то, что не попадёт. Что правка — говорит
  приложение: забыло — сводка промолчит;
— **доля приходит в учёт готовой** (`shares`), все расчёты «Месяца» —
  через один `toBase` → `flowOf` (Р-31); `account` у перевода — откуда.

Хвосты удобства «Месяца» и «Долгов» — в `03-План`, «Живые проверки»,
п. 7: делаются по моим находкам, а не наперёд (Р-48).

Для разбора Этапа 6 — что уже есть в модели: остаток не выводится
из операций (Р-05); снимок по частям и отложенный платёж (Р-35); счёт
истории `ledgerOnly` — итоги таблицы без выписок (Р-12, Р-14); переводы
между своими счетами одной записью (Р-12, Р-19); движения внутри счёта
не записываются, но объявляются сверкой выписки (Р-17); отметка
`loadedThrough` — по какое число доведена выписка (Р-26). В коде:
приход и расход счёта за период с переводами с обеих сторон уже
считает `turnoverOn` в `ledger/import.ts` (сверка выписки, Р-16);
капитал на дату — `capitalOn` (`capital/capital.ts`) и
`fullCapitalOn` (`summary/capital.ts`); модуль капитала учёта не знает —
сверка, которая знает и снимки, и записи, живёт в `summary/`.

Ключевое про работу:
— `src/shared` отсюда не правится; нехватка — промпт для сессии
  `FamilyCore` (как делать — `06-Процесс`, «Шаблоны»);
— `dbName: 'zloty'` и `v1Stores` заморожены; схема — версия 3. Новое
  хранилище — только миграцией с `additive: true`;
— данные извне — только через `zloty-import` (Р-09);
— число — вместе с основанием; тексты с числами — из констант;
— видимое изменение — строка в `src/changes.ts` тем же коммитом;
— личные данные и имена — только в `seed/`;
— правки файлов — скриптом-файлом из папки сессии;
— перед пушем: `npm test`, `npm run build`, `npm run smoke`,
  `npm run history`, `npm run capital` — по коду выхода. Новый шаг
  прогона проверяется порчей в `git worktree`.

Как работаем: одна развилка за раз, рекомендация — первой; плохую идею
называй до того, как я её выберу. Решения — в `docs/05-Решения.md`
в момент выбора. Сначала план работ, код после подтверждения. В конце —
Журнал, передача, коммит; перед `/clear` дерево чистое, в подмодуле тоже.
```
