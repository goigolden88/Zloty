# Процесс

Обновлён: 19.09.2026

Как ведётся работа над проектом. Не про то, что строим, а про то, как.

## Где что живёт

| Что | Где |
|---|---|
| Код приложения | `github.com/goigolden88/Zloty`, публичный; сайт — `goigolden88.github.io/Zloty/`. Локально — `C:\Ф\Prognananda\Claude Projs\Злотые` |
| Документы проекта | `docs/` в том же репозитории |
| Инструкции для Claude Code | `CLAUDE.md` в корне |
| Данные пользователя | IndexedDB на устройстве; с Этапа 1 — приватный репозиторий `ZlotyData` (заводит человек) |
| Таблица, выписки, копии, файлы импорта | `seed/` в корне — под `.gitignore`, в репозиторий не попадают никогда |
| Общее ядро и договор семьи | `github.com/goigolden88/FamilyCore`, публичный — эталон ядра. Здесь — подмодулем в `src/shared` с Этапа 0; свой клон для сессий ядра — `C:\Ф\Prognananda\Claude Projs\FamilyCore` |
| Приложения семьи | «Трапеза» `Еда\Trapeza`, «Делу Время» `ТаймМенеджмент\DeluVremya`, «Дневники» `Дневники\Dnevniki` — **только читать** |

Документы лежат вместе с кодом: `CLAUDE.md` подхватывается автоматически, документы версионируются заодно с кодом. Копия в чате — снимок; правится только репозиторий.

### Сессии Claude Code

Запускать из корня `Злотые`: история бесед привязана к рабочей папке. Сессия планирования 18–19.09.2026 шла отсюда же. Между этапами — `/clear`, `/compact` — для середины этапа. Перед `/clear`: дерево чистое, всё запушено, и по одному Журналу понятно, где мы и что дальше.

### Соседи — только читать

Из репозиториев «Трапезы», «Делу Время», «Дневников» и `FamilyCore` ничего не правится и не коммитится, даже найденная ошибка: о ней говорится человеку. **Сравнивать их код — через `git show`**, никогда по файлам с диска: `core.autocrlf=true` разворачивает рабочие копии в CRLF.

### Подмодуль ядра

С Этапа 0 (Р-03).

- **Клон** — только `git clone --recurse-submodules`; склонированное без него — `git submodule update --init`. Иначе `src/shared` пустая, и сборка падает непонятной ошибкой
- **`src/shared` отсюда не правится.** Ядру чего-то не хватает, в нём ошибка, `db` или `sync` не подходят под его типы без `as` — сказать человеку, что и где, дать промпт для сессии сопровождения `FamilyCore` (шаблон — в её `06-Процесс`) и остановиться на этом месте
- **Обновление ядра здесь** — `git submodule update --remote src/shared`, затем тесты, сборка, прогон по коду выхода и **коммит указателя**. Видимое изменение ядра — строка в `src/changes.ts` в том же коммите
- **Перед пушем** — `git status` чистый **и в подмодуле тоже**: `git -C src/shared status`
- **В `src/shared` не запускать `npm install`** — вторая копия React (Я-02 «FamilyCore»)
- **Тесты ядра здесь не гоняются, типы — проверяются** — как Р-52 «Трапезы»: `vitest` исключает `src/shared/**`, `tsc` проверяет его целиком
- Поломка в ядре чинится один раз, в `FamilyCore`

## Разделение работы

**Claude Code — и «что строить», и «как».** Продуктовые развилки и пересмотры архитектуры — тоже здесь: он видит настоящие файлы и проверяет факты. Режим планирования — когда обсуждаем и ничего не трогаем.

**Чат (Claude Desktop, claude.ai) — когда компьютера нет под рукой**, для свободного разговора без кода и для картинок. Итог приходит в репозиторий готовыми кусками markdown.

**Беседа с Claude — ещё и инструмент данных.** Выписки, скриншоты и файлы разбираются вне приложения по его промпту; в приложение приходит готовый JSON `zloty-import` (Р-08, Р-09). Выписки из беседы в репозиторий не кладутся — только в `seed/`.

### Планирование в Claude Code

1. Сессия планирования из корня репозитория — заготовка в «Шаблонах»
2. Решения — в `05-Решения` в момент выбора; правки `01`, `02`, `03` — по ходу
3. Конец: Журнал, передача, коммит документов, дерево чистое
4. `/clear` — и код новой сессией по плану. Контекст планирования в неё не переезжает: память — документы

## Обратный поток: код → обсуждение

То, что Claude Code в конце сессии называет «не описано в документах», возвращается в обсуждение, а не решается на месте.

## Правила ведения документов

- **Не менять задним числом.** Отмена решения — новая запись со ссылкой
- **Решения записываются в момент выбора**
- **Журнал обновляется в конце каждой сессии**
- **Перед пушем — `npm test`, `npm run build`, `npm run smoke`**, итог по коду выхода, а не по выводу
- **Прогон проверяется порчей**, когда меняется его сценарий: сломанная строка экрана обязана дать код 1. Порча — в `git worktree`, не в основной папке
- **Видимое изменение — строка в `src/changes.ts`** в том же коммите
- **План содержит только предстоящее**
- **Цифра с листа таблицы — вместе с формулой**, из которой она посчитана (опыт «Трапезы», Журнал 15.09.2026)
- **Правки файлов с кириллицей и замены с обратной косой чертой** — инструментом правки или скриптом-файлом из папки сессии, не heredoc Bash: он съедает `\` (опыт `FamilyCore`, Журнал 18.09.2026)

## Шаблоны

Сессия сопровождения `FamilyCore` — третий исход `ImportPlan` (отложено человеком 22.09.2026). Запускать **из корня `FamilyCore`**, не из «Злотых»:

```
Ядру не хватает третьего исхода у `ImportPlan`, и под этим три повода
из «Злотых» — не гипотеза.

Сейчас разбор раздела импорта даёт два исхода: запись попадёт в базу
(`writes`) или не попадёт (`issues`). Третьего — «попадёт, но человеку
стоит посмотреть» — нет, и приложения обходят это неправильно:

1. Сообщение считается отказом. «Злотые» пишут в `issues` заметку
   о склейке переводов, и сводка показывает «не попадёт в базу: 6», где
   шестое — не отказ, а сообщение. Человек видит завышенное число отказов
   и не понимает, какая запись потерялась.
2. Сторож живёт вне приложения. Проверка «расход в день выглядит
   неправдоподобно» реализована в `scripts/check-history.mjs`, потому
   что сказать её из разбора нечем: это не отказ, а подозрение.
3. Правка существующей записи не видна в сводке. Импорт «Злотых»
   помечает счёт полем `loadedThrough` из сошедшейся сверки (их Р-26).
   Это правка, а не заведение: в `added` ей не место, в `issues` — тоже,
   и сводка о ней молчит вовсе.

Что нужно: у `ImportPlan` — третий список, что-то вроде `notes`, для
записей, которые загружаются, но о которых надо сказать. Сводка
`ImportRecords` показывает его отдельным разделом, своим словом, и
не примешивает к счёту отказов. Правка существующих записей должна
называться отдельно от заведения новых.

Прочитай `docs/` ядра и реши, как это ложится на договор семьи: в ядре
четыре приложения, и `ImportPlan` общий. Решение — Я-NN в решениях ядра.
После релиза в «Злотых»: `git submodule update --remote src/shared`,
тесты, сборка, прогон, коммит указателя и строка в `src/changes.ts`.
```

Начало сессии по плану:

```
Работаем по docs/03-План.md, следующий этап оттуда.
Прочитай CLAUDE.md и docs/ перед началом.
Сначала покажи план работ, код пиши после подтверждения.
```

Сессия планирования:

```
Планируем: <что — новый этап, модуль, развилка>. Прочитай CLAUDE.md
и docs/. Код не пишем — обсуждаем по одной развилке, с твоей
рекомендацией, и записываем решения в docs/ в момент выбора.
В конце — Журнал, передача в 06-Процесс, коммит документов.
```

Конец сессии:

```
Дай текст для docs/04-Журнал.md: что сделано, в каком состоянии,
следующий шаг. И для docs/05-Решения.md, если по ходу что-то решили.
Отдельно — что осталось не описанным в документах.
```

### Передача в следующую сессию

Переписывается в конце каждого этапа. Сейчас — после закрытия Этапа 1 (22.09.2026).

**Этап 1 закрыт.** Обе живые проверки пройдены: синхронизация между двумя устройствами и настоящие выписки четырёх банков через импорт. Следующая сессия начинает Этап 2.

```
Проект «Злотые» — личный учёт денег, четвёртое приложение семьи
local-first приложений, первое, которое выросло на ядре `FamilyCore`.
Работать в корне репозитория `C:\Ф\Prognananda\Claude Projs\Злотые`.
Клон — только `git clone --recurse-submodules`.

Прочитай `CLAUDE.md` и все файлы `docs/` перед началом — особенно
`04-Журнал.md` (запись 21–22.09.2026) и решения Р-12, Р-16, Р-17,
Р-18, Р-19, Р-20. Модель в `02-Архитектура.md` окончательная; типы
и имена полей берутся оттуда, а не придумываются заново.

Состояние: Этапы 0 и 1 закрыты. В базе лежат настоящие данные —
девять периодов прежней таблицы и первый месяц из выписок четырёх
банков, 168 операций. Синхронизация работает между телефоном
и компьютером.
`npm test` 285, `npm run build`, `npm run smoke` 74 проверки,
`npm run history` 44 — всё зелёное по коду выхода.

Следующий — Этап 2, «Какой доход мне нужен»: необходимый доход,
рост обычного месяца, «что улучшить» markdown'ом и промптом.
Начинается он с хвоста Этапа 1: **регулярные ни разу не заведены.**
Шаблоны написаны и работают, но необходимый доход стоит на них,
так что первым делом — завести настоящие по загруженной выписке.

**Чему научила прошлая сессия.** Кода написано мало, решений принято
пять, и все пять родились из настоящих данных. Ни одну из найденных
ошибок не нашли бы тесты: они про то, что человек видит и во что
верит, а не про то, что код считает. Каждый заход выписки давал ровно
одну находку, и каждая была моей, а не беседы. Проси показать экран
после любого заметного изменения — и проси показать, что сказала
беседа: её отчёт «в чём я сомневаюсь» дважды указал на мой баг.

Что важно помнить про уже написанное:
— **сверка выписки (Р-16)** — раздел `checks`: беседа переписывает
  из выписки остатки, приложение считает по записям и сравнивает.
  Не сошлось или сверки нет — операции счёта не грузятся, разница
  названа суммой. Перевод проходит по сверке одной стороны;
— **всё, что не переносится, называется суммой (Р-17, Р-18)** —
  `skippedIn` и `skippedOut`: движения между своими договорами внутри
  банка и операции, чья дата вне периода выписки. Выписка приходит
  на договор, а счёт — банк целиком; учёт хранит дату операции,
  а выписка считает по дате обработки;
— **остаток главнее оборотов:** банки считают обороты по-разному,
  один показывает кэшбэк мимо «поступлений»;
— **склейки переводов нет (Р-19).** Одно движение — одна запись,
  и сводит её беседа. Приложение не угадывает: угадывание съедало
  настоящие записи, и это нашли только чтением кода;
— **`account` у перевода — счёт, ОТКУДА ушли деньги.** Это главная
  ошибка беседы: читая выписку банка, она пишет туда этот банк;
— **период владеет месяцем по большинству дней (Р-20).** Край,
  заходящий на день-другой, месяц не забирает, а задетые дни
  называются числом;
— **`ext` пишется один раз и не меняется** (Р-12, п. 4): загруженную
  операцию **правят, а не удаляют**;
— **счёт наугад не заводится** (Р-13), категория — заводится;
— **«с какого дня брать выписку» не считает переводы:** перевод
  пишется со стороны отправителя, откуда бы его ни прочитали.

Хвосты, записанные и не сделанные (`04-Журнал`, «на обсуждение»):
— **правка ядра, Я-NN, теперь с двумя поводами:** у `ImportPlan` нет
  третьего исхода — «загружено, но посмотрите». Из-за этого сводка
  считает сообщения отказами («не попадёт в базу: 6», где шестое —
  сообщение), а сторож на расход в день живёт в скрипте сверки,
  а не в приложении;
— сводка не показывает, какие категории заведёт, а удалить категорию
  потом нельзя — только архив;
— текущий месяц сравнивается с полными, и нигде не сказано, что он
  ещё идёт; «обычный месяц» посчитан по одному неполному августу;
— норма сбережений в сотнях процентов: доход месяца известен не весь;
— у валюты нет кнопки «Изменить»; с «Месяца» нельзя перейти
  к операциям месяца; «Неразобранные поступления» ищутся поиском,
  но отдельного места «разобрать» нет.

Ключевое про работу:
— `src/shared` отсюда не правится. Не хватает ядру — сказать мне и дать
  промпт для сессии `FamilyCore`, а не `as` и не копия файла. Уже
  найдено и отложено: выборки по индексам и третий исход `ImportPlan`;
— `dbName: 'zloty'` и `v1Stores` заморожены: восемь хранилищ, новое —
  только миграцией с `additive: true`;
— данные извне — только через `zloty-import` и экран импорта (Р-09);
— число — вместе с основанием; тексты с числами — из констант. Сторож
  цифр в справке падает на любой цифре в исходнике, но **пропавшее
  число он не ловит**: `plural` ядра возвращает форму слова, а не число
  вместе с ней;
— видимое изменение — строка в `src/changes.ts` тем же коммитом;
— личные данные — только в `seed/` под `.gitignore`; примеры в промптах
  и тестах выдуманные; в документах и коммитах — обобщённо;
— правки файлов с кириллицей — инструментом правки или скриптом
  в scratchpad; коммиты — Bash с heredoc; `gh` нет, итог деплоя —
  через api.github.com без токена;
— перед пушем: `npm test`, `npm run build`, `npm run smoke`,
  `npm run history` — итог по коду выхода. Новый экран — новые шаги
  прогона, и прогон проверяется порчей.

Как работаем: одна развилка за раз, рекомендация — первой; плохую идею
называй до того, как я её выберу. Решения — в `docs/05-Решения.md`
в момент выбора. Сначала план работ, код после подтверждения. В конце —
Журнал, передача, коммит; перед `/clear` дерево чистое, в подмодуле тоже.
```
