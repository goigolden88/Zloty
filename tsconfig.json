{
  // Настройки компилятора — семьи, из ядра: ядро внутри src/shared собирается
  // по тем же правилам, что у себя. `src` накрывает и подмодуль с его тестами —
  // tsc «Злотых» проверяет ядро против своих пакетов (Р-52 «Трапезы»).
  "extends": "./src/shared/tsconfig.base.json",
  "include": ["src", "vite.config.ts"]
}
