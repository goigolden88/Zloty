import { Link } from 'react-router-dom'
import { InstallNote } from '../shared/ui/Install.tsx'

/**
 * Приветствие при первом запуске: что это и с чего начать.
 *
 * Показывается, пока в базе нет ни одной записи и приветствие не закрыли.
 * Первая запись уберёт его сама — дело сделано; «Понятно» — насовсем.
 *
 * Числа — только из констант: цифрой в этом файле не пишется ничего, это
 * проверяет `help.test.ts` (CLAUDE.md, «Правила интерфейса»).
 */
export function Welcome({ onDone, empty }: { onDone: () => void; empty: boolean }) {
  return (
    <section className="panel block">
      <h2>С чего начать</h2>
      <p>
        Злотые — учёт денег: сколько уходит, сколько приходит и сколько из этого удалось отложить. Всё
        хранится на этом устройстве и работает без сети; без настройки наружу ничего не уходит.
      </p>
      <ul>
        <li>
          <b>Сейчас</b> — вносить ещё нечего: счета, операции и разбор выписок появятся на следующем шаге.
          Приложение уже можно поставить иконкой и проверить, что оно открывается без сети.
        </li>
        <li>
          <b>Синхронизация</b> — <Link to="/settings">«Настройки»</Link> → «Синхронизация»: свой приватный
          репозиторий и токен на каждое устройство. Можно настроить заранее.
        </li>
        <li>
          <b>Копия данных</b> — там же: файл, из которого всё восстанавливается.
        </li>
        <li>
          Как всё устроено — <Link to="/help">справка</Link>, «?» в шапке «Месяца».
        </li>
      </ul>

      <h3 className="unit__name">Установка</h3>
      <InstallNote empty={empty} />

      <div className="row">
        <button type="button" onClick={onDone}>
          Понятно
        </button>
      </div>
    </section>
  )
}
