import { Link } from 'react-router-dom'
import { days, timeSpan } from '../shared/core/dates.ts'
import { QUIET_MS } from '../shared/core/sync.ts'
import { MAX_DECIMALS } from '../modules/money/money.ts'
import { RATE_MAX_AGE_DAYS } from '../modules/money/rates.ts'
import { STALE_DAYS as BACKUP_DAYS } from '../shared/ui/backup.ts'
import { Fold } from '../shared/ui/Fold.tsx'

/**
 * Справка `#/help`: как устроено приложение. Вход — «?» в шапке «Месяца».
 *
 * Числа и правила — сроки, пороги, число знаков — берутся из тех же констант,
 * по которым работает код. Цифрой в этом файле не пишется ничего: это
 * проверяет `help.test.ts` (CLAUDE.md, «Правила интерфейса»). Смысл фраз
 * правится руками: появилась возможность — появилась строка о ней.
 *
 * Устройство разделов — «Трапезы», у неё — из «Делу Время» и «Дневников».
 */
export function Help() {
  return (
    <>
      <header className="screen-head">
        <h1>Справка</h1>
        <p className="muted">Коротко о том, как всё устроено. Тап по вопросу — ответ.</p>
      </header>

      <Fold id="help:overview" title="Что уже есть, а чего ещё нет" folded>
        <p>
          Сейчас в приложении есть оболочка: оно ставится иконкой, открывается без сети, умеет
          синхронизацию и копию данных. Счетов, операций и ответа «сколько я отложил» пока нет — они
          следующим шагом.
        </p>
        <p className="muted">
          Пустой экран «Месяц» так и говорит, вместо того чтобы показывать нули: ноль означал бы «ничего
          не потрачено», а это неправда.
        </p>
      </Fold>

      <Fold id="help:money" title="Как считаются деньги" folded>
        <ul>
          <li>
            Сумма хранится целым числом в минимальных единицах валюты — в копейках, центах, сатоши. Так
            сложение не копит ошибку, которая появляется у дробных чисел.
          </li>
          <li>
            Знака у суммы нет: расход это, доход или перевод между своими счетами — говорит вид записи.
          </li>
          <li>
            Валюта — запись справочника: код, название, сколько знаков после запятой и в чём показывать.
            Знаков бывает не больше, чем {MAX_DECIMALS}: дальше не хватит точности чисел.
          </li>
          <li>
            Курс берётся на дату события — операции или снимка, а не сегодняшний. Курса на дату нет —
            берётся ближайший более ранний, не старше чем {days(RATE_MAX_AGE_DAYS)}. Нет и такого — сумма
            в итог не входит, и приложение говорит, какого курса не хватило, вместо того чтобы показать
            ноль.
          </li>
          <li>Пересчёт в другую валюту нигде не хранится: поправите курс — поменяется и число.</li>
        </ul>
      </Fold>

      <Fold id="help:accounts" title="Счета, операции и остатки" folded>
        <ul>
          <li>
            <b>Счёт</b> — банк целиком, а не отдельная карта: выписка банка ложится на один счёт, а
            перекидывания между своими картами и вкладами внутри банка в учёт не попадают — они ничего не
            меняют.
          </li>
          <li>
            <b>Остаток</b> не считается из операций: его приносит снимок — «на такое-то число было
            столько-то». Снимок можно разбить на части счёта: сколько на карте, сколько на накопительном.
          </li>
          <li>
            <b>Итог периода</b> — запись «за такой-то период потрачено столько-то» там, где отдельных
            операций нет. По месяцам он не делится: месяц, накрытый чужим периодом, так и говорит, каким.
          </li>
          <li>
            <b>Долги</b> — не расход и не доход. Оплата за компанию войдёт в расход только своей долей,
            а возврат долга доходом не станет.
          </li>
        </ul>
      </Fold>

      <Fold id="help:sync" title="Синхронизация и копия" folded>
        <ul>
          <li>
            Записи хранятся на устройстве. Синхронизация — свой приватный репозиторий на GitHub: устройства
            обмениваются через него, сервера у приложения нет.
          </li>
          <li>
            Проход идёт через {timeSpan(QUIET_MS)} после последнего действия, при открытии и при возврате
            сети. Одна и та же запись, правленная на двух устройствах, разрешается по времени правки:
            побеждает поздняя.
          </li>
          <li>
            Копия файлом — в <Link to="/settings">«Настройках»</Link>. Если копию не забирали дольше чем{' '}
            {days(BACKUP_DAYS)}, приложение напомнит.
          </li>
          <li>Токен хранится в браузере этого устройства и наружу не уходит. У каждого устройства свой.</li>
        </ul>
      </Fold>

      <Fold id="help:trouble" title="Если что-то не так" folded>
        <ul>
          <li>
            Приложение не обновилось — закройте и откройте его. Установленное проверяет обновление при
            запуске.
          </li>
          <li>
            Синхронизация ругается — проверьте токен и имя репозитория в «Настройках»: чаще всего дело
            в сроке токена.
          </li>
          <li>Что-то сломалось — в «Настройках», в разделе «О приложении», есть журнал ошибок.</li>
        </ul>
      </Fold>
    </>
  )
}
